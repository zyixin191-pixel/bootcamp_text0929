
name: Bootcamp_Text_Workflow

on:
  push:
    branches: [ luch ]
  pull_request:
    branches: [ luch ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: dotnet restore BootcampText4.sln
    - name: Build
      run: dotnet build BootcampText4.sln --no-restore
    - name: Test
      run: dotnet test BootcampText4.sln --no-build --verbosity normal
